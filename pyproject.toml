[project]
name = "dask-clusters-test"
version = "0.1.0"
description = "Test codes for the Dask Clusters"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
    "black>=25,<26",
    "dask[complete]>=2025,<2026",
    "ipython>=8,<10",
    "jupyter>=1,<2",
    "matplotlib>=3,<4",
    "numpy>=2,<3",
    "xarray>=2025,<2026",
    "zarr>=2,<3",
]
